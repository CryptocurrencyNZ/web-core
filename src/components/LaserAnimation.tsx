import { useEffect } from "react";

function LaserAnimation() {
  useEffect(() => {
    // Laser animation logic using GSAP
  }, []);

  return null; // This component handles animations only
}

export default LaserAnimation;
